clc
clear all
close all
radius=50;
lamda=500;
m=1.5;
x=2*pi*radius/lamda;
degerler=Mie(m,x);
Qsca=degerler(2);
g=degerler(5)
sayi=361;

scat_ph_fn=Mie_phasefn(m, x, sayi)/(x*x*Qsca);%scattering phase function
% figure
% plot(linspace(0,180,sayi),scat_ph_fn/max(scat_ph_fn))
theta=linspace(0,180,sayi);
trapz(linspace(0,1,sayi),scat_ph_fn.*cos())